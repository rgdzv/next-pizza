import type { SortListType } from '../types/sortListType'

export const SORTLIST: SortListType[] = [
    { name: 'рейтингу', sortProperty: 'rating' },
    { name: 'алфавиту', sortProperty: 'title' }
]
